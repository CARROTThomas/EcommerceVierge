{% extends 'base.html.twig' %}

{% block title %}Edit Order{% endblock %}

{% block body %}
    <h1>Edit Order</h1>

    {{ include('my_order/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary mt-1" href="{{ path('app_order_index') }}">back to list</a>

{% endblock %}
